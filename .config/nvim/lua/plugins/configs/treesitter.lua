local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "bash", "markdown", "regex" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
